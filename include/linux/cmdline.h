#ifndef INCLUDE_LINUX_COMMAND_LINE_PARAM_H
#define INCLUDE_LINUX_COMMAND_LINE_PARAM_H

#include <stddef.h>
#include <stdbool.h>
/*
typedef struct {
    bool        enable;          // 参数是否有效
    size_t      width;           // 宽度
    size_t      height;          // 高度
    char        path[256];       // 路径
    int         mode;            // 模式
    int         type;            // 类型
    int         calib;           // 是否启用矫正
    int         flip;            // 镜像模式(0, 1, 2, 3)
} cmdline_param_t;*/

#endif
